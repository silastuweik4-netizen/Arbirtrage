{
  "name": "arb-bot",
  "version": "1.0.0",
  "description": "Arbitrage bot with Hardhat deployment on Base",
  "main": "detector.js",
  "scripts": {
    "start": "node detector.js",
    "deploy": "npx hardhat run scripts/deploy.js --network base",
    "test": "npx hardhat test"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^16.3.1",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "hardhat": "^2.20.1",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  }
}
