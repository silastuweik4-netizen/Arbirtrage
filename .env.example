# Base Chain RPC Endpoint
# Get free RPC from: Infura, Alchemy, QuickNode, or use public endpoint
RPC_URL=https://mainnet.base.org

# Discord/Slack Webhook URL for Arbitrage Alerts (Optional)
# Discord: Server Settings → Integrations → Webhooks → New Webhook
# Slack: https://api.slack.com/messaging/webhooks
WEBHOOK_URL=

# Minimum price difference threshold to trigger alert (in percentage)
# Example: 0.5 means 0.5% price difference
PRICE_DIFFERENCE_THRESHOLD=0.5

# Scan interval in milliseconds
# Example: 10000 = 10 seconds
CHECK_INTERVAL_MS=10000

# Trade size for simulation (in token0 units)
# Example: 100 means simulate swapping 100 units of token0
TRADE_SIZE=100

# Minimum liquidity in USD to consider a pool valid
# Example: 10000 means $10,000 minimum liquidity
MIN_LIQUIDITY_USD=10000

# Private key for future execution (Optional - NOT REQUIRED FOR DETECTION)
# WARNING: Never commit this to GitHub!
PRIVATE_KEY=
