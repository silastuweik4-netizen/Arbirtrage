<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arbitrum Arb Bot Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #121212; color: #e0e0e0; }
        .card { background-color: #1e1e1e; border: 1px solid #333; color: #e0e0e0; }
        .table { color: #e0e0e0; }
        .profit-badge { background-color: #2e7d32; color: white; }
        .status-badge { background-color: #0277bd; color: white; }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Arbitrum Arbitrage Bot</h1>
            <div>
                <span class="badge status-badge p-2">Status: <%= data.status %></span>
                <span class="badge bg-secondary p-2">Last Scan: <%= data.timestamp || 'Never' %></span>
            </div>
        </div>

        <div class="card p-4">
            <h3>Live Opportunities</h3>
            <hr>
            <% if (data.opportunities.length > 0 ) { %>
                <div class="table-responsive">
                    <table class="table table-dark table-hover">
                        <thead>
                            <tr>
                                <th>Token</th>
                                <th>Spread</th>
                                <th>Buy On</th>
                                <th>Sell On</th>
                                <th>Buy Price</th>
                                <th>Sell Price</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% data.opportunities.forEach(opp => { %>
                                <tr>
                                    <td><strong><%= opp.token %></strong></td>
                                    <td><span class="badge profit-badge"><%= opp.spread %>%</span></td>
                                    <td><%= opp.buy.name %></td>
                                    <td><%= opp.sell.name %></td>
                                    <td>$<%= opp.buy.price.toFixed(4) %></td>
                                    <td>$<%= opp.sell.price.toFixed(4) %></td>
                                    <td>
                                        <a href="<%= opp.buy.url %>" target="_blank" class="btn btn-sm btn-outline-primary">Buy</a>
                                        <a href="<%= opp.sell.url %>" target="_blank" class="btn btn-sm btn-outline-danger">Sell</a>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="alert alert-info">No profitable opportunities found in the last scan. Waiting for next cycle...</div>
            <% } %>
        </div>

        <div class="mt-4 text-center text-muted">
            <small>Scanning 30+ tokens every 5 minutes using DexScreener Free API.</small>
        </div>
    </div>

    <script>
        setTimeout(() => { location.reload(); }, 60000);
    </script>
</body>
</html>
