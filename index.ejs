<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arbitrum Arb Bot Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #121212; color: #e0e0e0; }
        .card { background-color: #1e1e1e; border: 1px solid #333; color: #e0e0e0; }
        .table { color: #e0e0e0; }
        .profit-badge { background-color: #2e7d32; color: white; }
        .status-badge { background-color: #0277bd; color: white; }
        .recommendation-text { color: #81c784; font-weight: bold; }
        .warning-text { color: #ffb74d; font-size: 0.85rem; }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Arbitrum Arbitrage Bot <small class="text-muted fs-6">v2.0</small></h1>
            <div>
                <span class="badge status-badge p-2">Status: <%= data.status %></span>
                <span class="badge bg-secondary p-2">Last Scan: <%= data.timestamp || 'Never' %></span>
            </div>
        </div>

        <div class="card p-4">
            <div class="d-flex justify-content-between align-items-center">
                <h3>Live Opportunities</h3>
                <span class="text-muted small">Auto-calculating optimal trade sizes for < 0.1% slippage</span>
            </div>
            <hr>
            <% if (data.opportunities.length > 0) { %>
                <div class="table-responsive">
                    <table class="table table-dark table-hover align-middle">
                        <thead>
                            <tr>
                                <th>Token</th>
                                <th>Spread</th>
                                <th>Optimal Size</th>
                                <th>Est. Net Profit</th>
                                <th>Buy/Sell DEX</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% data.opportunities.forEach(opp => { %>
                                <tr>
                                    <td><strong><%= opp.token %></strong></td>
                                    <td><span class="badge profit-badge"><%= opp.spread %>%</span></td>
                                    <td>
                                        <span class="recommendation-text">$<%= opp.optimalSize %></span><br>
                                        <span class="warning-text">Max for low slippage</span>
                                    </td>
                                    <td>
                                        <span class="<%= opp.expectedProfit > 0 ? 'text-success' : 'text-danger' %>">
                                            $<%= opp.expectedProfit %>
                                        </span>
                                        <div class="text-muted x-small" style="font-size: 0.7rem;">After 0.6% fees</div>
                                    </td>
                                    <td>
                                        <small>Buy: <%= opp.buy.name %></small><br>
                                        <small>Sell: <%= opp.sell.name %></small>
                                    </td>
                                    <td>
                                        <div class="btn-group">
                                            <a href="<%= opp.buy.url %>" target="_blank" class="btn btn-sm btn-primary">Buy</a>
                                            <a href="<%= opp.sell.url %>" target="_blank" class="btn btn-sm btn-danger">Sell</a>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="alert alert-info">No profitable opportunities found in the last scan. Waiting for next cycle...</div>
            <% } %>
        </div>

        <div class="mt-4 row">
            <div class="col-md-6">
                <div class="card p-3 small text-muted">
                    <strong>Slippage Protection:</strong> Optimal size is capped at 0.5% of pool liquidity to ensure your trade doesn't move the price against you.
                </div>
            </div>
            <div class="col-md-6">
                <div class="card p-3 small text-muted">
                    <strong>Net Profit Calculation:</strong> Estimated profit accounts for a standard 0.6% round-trip DEX fee.
                </div>
            </div>
        </div>
    </div>

    <script>
        setTimeout(() => { location.reload(); }, 60000);
    </script>
</body>
</html>
